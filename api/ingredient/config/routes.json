{
  "routes": [
    {
      "method": "GET",
      "path": "/ingredients",
      "handler": "Ingredient.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/ingredients/count",
      "handler": "Ingredient.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/ingredients/:id",
      "handler": "Ingredient.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/ingredients",
      "handler": "Ingredient.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/ingredients/:id",
      "handler": "Ingredient.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/ingredients/:id",
      "handler": "Ingredient.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
